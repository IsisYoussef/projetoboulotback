<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="light">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('dashboard') }}">O'Boulot</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Les Offres</a>
					<div class="dropdown-menu">
						<a class="nav-link" href="{{ path('app_back_job_index') }}">Voir la liste</a>
						<a class="nav-link" href="{{ path('app_back_job_new') }}">Ajouter</a>
					</div>
				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Les candidats</a>
					<div class="dropdown-menu">
						<a class="nav-link" href="{{ path('app_back_candidate_index') }}">Voir la liste</a>
						<a class="nav-link" href="{{ path('app_back_candidate_new') }}">Ajouter</a>
					</div>
				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Les entreprises</a>
					<div class="dropdown-menu">
						<a class="nav-link" href="{{ path('app_back_company_index') }}">Voir la liste</a>
						<a class="nav-link" href="{{ path('app_back_company_new') }}">Ajouter</a>
					</div>
				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Equipe</a>
					<div class="dropdown-menu">
						<a class="nav-link" href="{{ path('app_back_team_index') }}">Voir la liste</a>
						<a class="nav-link" href="{{ path('app_back_team_new') }}">Ajouter</a>
					</div>
				</li>
			</ul>
			<form class="d-flex">
				<input class="form-control me-sm-2" type="search" placeholder="Search">
				<button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
			</form>
		</div>
		{% if is_granted("IS_AUTHENTICATED_FULLY")%}
                    <a href="{{ path('app_logout') }}"><button type="button" class="btn btn-primary me-2">Deconnexion</button></a>
                {% else %}
                    <a href="{{ path('app_login') }}"><button type="button" class="btn btn-primary me-2">Connexion</button></a>
                {% endif %}
	</div>
</nav>
