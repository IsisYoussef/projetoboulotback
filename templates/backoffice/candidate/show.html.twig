{% extends 'base.html.twig' %}

{% block title %}Candidat{% endblock %}

{% block body %}
	<h1>Candidat</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ candidate.id }}</td>
			</tr>
			<tr>
				<th>Email</th>
				<td>{{ candidate.email }}</td>
			</tr>
			<tr>
				<th>Roles</th>
				<td>{{ candidate.roles ? candidate.roles|json_encode : '' }}</td>
			</tr>
			<tr>
				<th>Mot de passe</th>
				<td>{{ candidate.password }}</td>
			</tr>
			<tr>
				<th>Civilité</th>
				<td>{{ candidate.gender }}</td>
			</tr>
			<tr>
				<th>Prénom</th>
				<td>{{ candidate.firstname }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ candidate.lastname }}</td>
			</tr>
			<tr>
				<th>Téléphone</th>
				<td>{{ candidate.phone }}</td>
			</tr>
			<tr>
				<th>Adresse</th>
				<td>{{ candidate.address }}</td>
			</tr>
			<tr>
				<th>Code Postal</th>
				<td>{{ candidate.postalCode }}</td>
			</tr>
			<tr>
				<th>Ville</th>
				<td>{{ candidate.city }}</td>
			</tr>
			<tr>
				<th>Présentation</th>
				<td>{{ candidate.presentation }}</td>
			</tr>
			<tr>
				<th>Date de création</th>
				<td>{{ candidate.createdAt ? candidate.createdAt|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
			<tr>
				<th>Date de mis à jour</th>
				<td>{{ candidate.updatedAt ? candidate.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
			</tr>

			<tr>
				<th>Date de naissance</th>
				<td>{{ candidate.birthday ? candidate.birthday|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
			<tr>
				<th>Avatar</th>
				<td>{{ candidate.avatar }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_back_candidate_index') }}">Retour à la liste</a>

	<a href="{{ path('app_back_candidate_edit', {'id': candidate.id}) }}">Modifier</a>

	{{ include('backoffice/candidate/_delete_form.html.twig') }}
{% endblock %}
