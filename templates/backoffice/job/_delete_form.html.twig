<form method="post" action="{{ path('app_back_job_delete', {'id': job.id}) }}" onsubmit="return confirm('Etes-vous sur(e) de vouloir supprimer cette offre');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job.id) }}">
    <button class="btn">Supprimer</button>
</form>
