<form method="post" action="{{ path('app_back_team_delete', {'id': user.id}) }}" onsubmit="return confirm('Etes vous sur(e) de vouloir supprimer cela?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn">Supprimer</button>
</form>
